<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_form_alumno" model="ir.ui.view">
        <field name="name">autoescuela.alumno.form</field>
        <field name="model">autoescuela.alumno</field>
        <field name="arch" type="xml">
            <form string="Alumno">
                <sheet>
                    <group>
                        <field name="nombre"/>
                        <field name="apellidos"/>
                        <field name="dni"/>
                        <field name="telefono"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_tree_alumno" model="ir.ui.view">
        <field name="name">autoescuela.alumno.tree</field>
        <field name="model">autoescuela.alumno</field>
        <field name="arch" type="xml">
            <tree string="Alumnos">
                <field name="nombre"/>
                <field name="apellidos"/>
                <field name="dni"/>
                <field name="telefono"/>
            </tree>
        </field>
    </record>

    <menuitem id="menu_autoescuela_root" name="Autoescuela" sequence="1"/>

    <menuitem id="menu_alumnos" name="Alumnos" parent="menu_autoescuela_root" sequence="2"/>
    
    <record id="action_alumno" model="ir.actions.act_window">
        <field name="name">Alumnos</field>
        <field name="res_model">autoescuela.alumno</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
